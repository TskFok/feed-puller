# 设置页 — 页面覆盖

继承 `design-system/MASTER.md`。

## 布局

- 网格：`.settings-grid`（大屏两列，小屏单列）
- 面板：`.settings-panel` 玻璃卡片
- 全宽区块：`.feishu-setup-guide`（`grid-column: 1 / -1`）

## 外观

- 主题切换：`.theme-toggle-group` / `.theme-toggle-btn`
- 选项：玻璃暗色、玻璃浅色、跟随系统

## 飞书

- 绑定弹窗：`AnimatedModal` + `panelClassName="bind-feishu-modal"` + 扫码容器 `.feishu-qr-inline`
